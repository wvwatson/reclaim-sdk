import { Provider } from '../types';
type GoDaddyLoginParams = {
    emailAddress: string;
    region: string;
};
type GoDaddyLoginSecretParams = {
    cookieStr: string;
    encQueryParams: string;
};
declare const goDaddyLogin: Provider<GoDaddyLoginParams, GoDaddyLoginSecretParams>;
export default goDaddyLogin;
