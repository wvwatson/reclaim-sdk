import { Provider } from '../../types';
import { GithubClaimType, SearchQueryObject } from './utils';
type GithubParams<T extends GithubClaimType> = {
    /** github `url` type eg: `commits` */
    type: T;
    /** repository name eg: {owner}/{repo} */
    repository: string;
    /** query string for github search */
    searchQuery: SearchQueryObject;
};
type GithubSecretParams = {
    token: string;
};
declare const makeGithubProvider: <T extends "github-issues" | "github-commits" | "github-pull-requests">() => Provider<GithubParams<T>, GithubSecretParams>;
export default makeGithubProvider;
