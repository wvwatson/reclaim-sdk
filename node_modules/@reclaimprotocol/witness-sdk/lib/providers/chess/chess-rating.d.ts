/**
 * For Chess.com app users to prove their Rating on Chess.com
 *
 * https://www.chess.com/
 */
import { Provider } from '../../types';
type ChessRatingParams = {
    rating: string;
    userName: string;
};
type ChessRatingSecretParams = {
    cookie: string;
};
declare const chessRating: Provider<ChessRatingParams, ChessRatingSecretParams>;
export default chessRating;
