import { Provider } from '../../types';
type IrsParams = {
    address: string;
};
type IrsSecretParams = {
    token: string;
};
declare const irsAddress: Provider<IrsParams, IrsSecretParams>;
export default irsAddress;
