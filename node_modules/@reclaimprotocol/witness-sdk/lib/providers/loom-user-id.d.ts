import { Provider } from '../types';
type LoomUserIdParams = {
    userId: string;
};
type LoomLoginSecretParams = {
    /** cookie string for authentication */
    cookieStr: string;
};
declare const loomUser: Provider<LoomUserIdParams, LoomLoginSecretParams>;
export default loomUser;
