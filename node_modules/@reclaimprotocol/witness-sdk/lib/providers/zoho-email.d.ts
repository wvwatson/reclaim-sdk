import { Provider } from '../types';
type ZohoParams = {
    email: string;
};
type ZohoSecretParams = {
    token: string;
};
declare const zohoEmail: Provider<ZohoParams, ZohoSecretParams>;
export default zohoEmail;
