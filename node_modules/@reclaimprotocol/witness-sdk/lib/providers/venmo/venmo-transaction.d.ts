import { Provider } from '../../types';
type VenmoTransactionParams = {
    userId: string;
    recipientId: string;
    amount: string;
};
type VenmoLoginSecretParams = {
    /** cookie string for authentication */
    cookieStr: string;
    userId: string;
};
declare const venmoTransaction: Provider<VenmoTransactionParams, VenmoLoginSecretParams>;
export default venmoTransaction;
