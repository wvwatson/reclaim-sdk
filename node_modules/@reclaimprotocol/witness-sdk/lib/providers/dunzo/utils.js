"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deliveredCondition = void 0;
function deliveredCondition(order) {
    return order.state === 'COMPLETED';
}
exports.deliveredCondition = deliveredCondition;
function getTopOrderValues(orderList, condition, k = 1) {
    var _a, _b, _c, _d;
    const result = [];
    let count = 0;
    for (const order of orderList) {
        if (condition(order)) {
            const orderVal = (_d = (_c = (_b = (_a = order.taskData) === null || _a === void 0 ? void 0 : _a.taskProperties) === null || _b === void 0 ? void 0 : _b.paymentInvoice) === null || _c === void 0 ? void 0 : _c.totalAmount) === null || _d === void 0 ? void 0 : _d.value;
            result.push(orderVal);
            count++;
            if (count === k) {
                break;
            }
        }
    }
    return result;
}
exports.default = getTopOrderValues;
