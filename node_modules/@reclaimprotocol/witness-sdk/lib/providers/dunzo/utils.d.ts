type DunzoOrder = {
    state: string;
    taskData: {
        taskProperties: {
            paymentInvoice: {
                totalAmount: {
                    value: number;
                };
            };
        };
    };
};
declare function deliveredCondition(order: DunzoOrder): boolean;
export default function getTopOrderValues(orderList: DunzoOrder[], condition: any, k?: number): number[];
export { deliveredCondition, };
