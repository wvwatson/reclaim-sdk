import { HashAlgorithm } from '../types';
import { SUPPORTED_CIPHER_SUITE_MAP } from './constants';
type DeriveTrafficKeysOptions = {
    masterSecret: Uint8Array;
    /** used to derive keys when resuming session */
    earlySecret?: Uint8Array;
    cipherSuite: keyof typeof SUPPORTED_CIPHER_SUITE_MAP;
    /** list of handshake message to hash; or the hash itself */
    hellos: Uint8Array[] | Uint8Array;
    /** type of secret; handshake or provider-data */
    secretType: 'hs' | 'ap';
};
export type SharedKeyData = Awaited<ReturnType<typeof computeSharedKeys>>;
export declare function computeUpdatedTrafficMasterSecret(masterSecret: Uint8Array, cipherSuite: keyof typeof SUPPORTED_CIPHER_SUITE_MAP): Promise<Uint8Array>;
export declare function computeSharedKeys({ hellos, masterSecret: masterKey, cipherSuite, secretType, earlySecret }: DeriveTrafficKeysOptions): Promise<{
    masterSecret: Uint8Array;
    clientSecret: Uint8Array;
    serverSecret: Uint8Array;
    clientEncKey: CryptoKey;
    serverEncKey: CryptoKey;
    clientIv: Uint8Array;
    serverIv: Uint8Array;
}>;
export declare function deriveTrafficKeys({ masterSecret, cipherSuite, hellos, secretType, }: DeriveTrafficKeysOptions): Promise<{
    masterSecret: Uint8Array;
    clientSecret: Uint8Array;
    serverSecret: Uint8Array;
    clientEncKey: CryptoKey;
    serverEncKey: CryptoKey;
    clientIv: Uint8Array;
    serverIv: Uint8Array;
}>;
export declare function deriveTrafficKeysForSide(masterSecret: Uint8Array, cipherSuite: keyof typeof SUPPORTED_CIPHER_SUITE_MAP): Promise<{
    masterSecret: Uint8Array;
    encKey: CryptoKey;
    iv: Uint8Array;
}>;
export declare function hkdfExtractAndExpandLabel(algorithm: HashAlgorithm, secret: Uint8Array, label: string, context: Uint8Array, length: number): Promise<Uint8Array>;
export declare function getHash(msgs: Uint8Array[] | Uint8Array, cipherSuite: keyof typeof SUPPORTED_CIPHER_SUITE_MAP): Promise<Uint8Array>;
export {};
